# Bump Buddy Docs
Documentation for Bump Buddy

## Run locally

Install `docsify-cli`:
```bash
npm i docsify-cli -g
```

Then use the following command to deploy it locally
```
docsify serve ./docs
```